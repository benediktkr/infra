---

- name: copy config
  copy:
    src: private/sudoisbot.yml
    dest: /etc/sudoisbot.yml

- name: install with pip from github (ugh)
  pip:
    name: git+https://github.com/benediktkr/sudoisbot.git
    state: latest
    executable: "{{ item }}"
  with_items:
    - pip
    - pip3
