# Run this playbook with -K to ask for sudo pass

- hosts: burrow
  become: true
  connection: local
  vars:
    - is_local: true
    - myusername: lehmann
    - dotfiles_path: "~lehmann/projects/dotfiles"
    - hostname: burrow
  roles:
    - common
    - laptop
    - dotfiles
