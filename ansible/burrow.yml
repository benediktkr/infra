# Run this playbook with -K to ask for sudo pass

- hosts: laptop
  become: true
  connection: local
  vars:
    - is_local: true
    - myusername: lehmann
    - dotfiles_path: "~/projects/dotfiles"
  roles:
    - common
    - laptop
    - dotfiles
