# Run this playbook with -K to ask for sudo pass

- hosts: burrow
  become: true
  #connection: local
  remote_user: root
  vars:
    - is_local: true
    - myusername: lehmann
    - dotfiles_path: "~lehmann/projects/dotfiles"
    - hostname: dontpanic
  roles:
    - common
    - laptop
    - dotfiles
