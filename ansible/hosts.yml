all:
  vars:
    ansible_user: root
    ansible_python_interpreter: /usr/bin/python3

  children:

    webserver:
      hosts:
        f.sudo.is:

    nextcloud:
      hosts:
        f.sudo.is:

    vpn:
      hosts:
        lon-vpn.sudo.is:
        c.sudo.is:

    pi:
      hosts:
        pi1.sudo.is:
          ansible_host: 192.168.1.2

    pihole:
      hosts:
        pi1.sudo.is:
