- hosts: freespace.sudo.is
  remote_user: root
  become: true
  vars:
    - myusername: benedikt
    - nextcloud_root: "/srv/nextcloud"
    - nextcloud_url: "nextcloud.sudo.is"
    - hostname: freespace 
  roles:
    - common
    - docker
    - dotfiles
    - nextcloud
