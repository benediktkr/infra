---

dependencies:
  - role: common-misc
    tags: common-misc

  - role: common-letsencrypt
    tags: common-letsencrypt

  - role: common-users
    tags: common-users

  - role: postfix
    tags: postfix

  # questionable if this one should remain
  - role: dotfiles
    tags: dotfiles

  # condially common
  - role: fde-dropbear
    tags: fde
    when: inventory_hostname in groups["fde"]

