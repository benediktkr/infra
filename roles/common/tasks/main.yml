---
# file: roles/common/tasks/main.yml
- include: common.yml
  tags:
    - common

- include_role:
    name: users
  tags:
    - users

- include_role:
    name: tunnel
  when: ssh_tunnels|length > 0
  tags:
    - tunnel
    - autossh

- include_role:
    name: dotfiles
  tags: dotfiles

- include_role:
    name: postfix
  tags:
    - postfix
    - postfix-cron

- include_role:
    name: telegraf
  tags: telegraf
  when: enable_telegraf == True
