---

- name: reload nginx
  service:
    name: nginx
    state: reloaded

- name: restart owntone container
  docker_container:
    name: owntone
    state: started
    restart: true
  when:
    - owntone_restart_handler|default(true)
