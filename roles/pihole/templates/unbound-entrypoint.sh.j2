#!/bin/bash

set -e

echo "setting uid for _unbound user to same as pihole: {{ pihole_uid }}"

groupmod -g {{ pihole_gid }} _unbound
usermod -u {{ pihole_uid }} -g {{ pihole_gid }} _unbound

exec /unbound.sh
