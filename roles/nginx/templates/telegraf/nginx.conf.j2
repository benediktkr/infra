# {{ ansible_managed }}

[[inputs.nginx]]
    urls = [
{%      if false and 'address' in ansible_default_ipv4 %}
        "http://{{ inventory_hostname }}/server_status"
{%      else %}
        "http://localhost/server_status"
{%      endif %}
    ]
